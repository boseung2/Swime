package com.swime.controller;

public class BoardController {
    // 모임찾기 페이지
    // 1. 가장 최근에 생성된 날짜순으로 모임을 정렬해서 뿌려준다.
    // 2. 사용자가 지역, 언어, 모임명으로 검색시 검색조건에 맞는 모임을 최근 생성된 날짜순으로 뿌려준다.

    // 모임 만들기 페이지
    // 1. 사진을 제외한 모든 카테고리가 입력되어있지않으면 알림창을 띄워준다. (프론트)
    // 2. 사용자가 모두 입력한 뒤에 유효성 검사를 진행해서 맞지않으면 알림창 띄워준다.
    // 3. 사진을 제외한 모든 데이터가 정상적으로 입력되면 db에 추가해준다.

    // 모임 수정 페이지

}
